#include "libft.h"

char    *ft_strtrim(char const *s1, char const *set)
{
	size_t	i;
	char	*str;

	i = 0;

	while ( s1[i] != '\0' || s1[i] == ft_strchr(set) )
		i++
	while ( s1[len] == ft_strchr(set) || len > 0)
		len--;

	
}